# 安全说明

## 凭据管理

- 不要把微博账号凭据、token、API key、Cookie、密码或私钥提交到仓库。
- 微博授权由本机 `weibo-cli` 管理。
- VibeSocial Agent 不保存微博 token，也不要求把 token 写入项目文件。
- 不要把 `.env`、凭据导出文件或包含授权信息的日志发送给他人。

## 本地状态

`.vibesocial/` 包含草稿、审核历史、Writing Memory 和发布记录，属于项目本地私有状态。它已加入 `.gitignore`，不要强制提交。

## 发现问题

如果发现凭据泄露、隐私边界失效或发布确认被绕过，请先停止相关发布操作，并在提交问题时删除 token、账号标识、私人文本和本地路径。
